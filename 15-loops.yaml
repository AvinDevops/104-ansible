- name: loops
  hosts: localhost
  tasks: # list of tasks/collections/modules
  - name: printing loops
    ansible.builtin.debug:
      msg: "{{items}}"
    loops:
    - ansible
    - mysql
    - postfix